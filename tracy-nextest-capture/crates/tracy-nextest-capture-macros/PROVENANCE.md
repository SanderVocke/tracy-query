# Provenance

`tracy-nextest-capture-macros` is repository-owned code first implemented for
`tracy-extensions`; it is not derived from a published third-party proc-macro crate.
It expands supported synchronous libtest functions into calls to the adjacent
`tracy-nextest-capture` lifecycle crate and is not published to crates.io.
